NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
# In production, leave this unset and route /api/v1 on each tenant subdomain to the same backend.
NEXT_PUBLIC_APP_DOMAIN=localhost
NEXT_PUBLIC_RAZORPAY_KEY_ID=
