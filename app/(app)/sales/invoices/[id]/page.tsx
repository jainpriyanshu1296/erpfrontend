'use client';
import { useParams } from 'next/navigation';
import { RecordDetail } from '@/components/record-detail';
import { WorkflowAction } from '@/components/workflow-action';
export default function Page() { const {id}=useParams<{id:string}>(); return <div className="space-y-6"><RecordDetail title="Sales invoice" endpoint={`/sales/invoices/${id}`} backHref="/sales/invoices" fields={[{key:'invoice_number',label:'Invoice number'},{key:'company_name',label:'Customer'},{key:'sales_order_id',label:'Sales order'},{key:'invoice_date',label:'Invoice date'},{key:'due_date',label:'Due date'},{key:'total_amount',label:'Total amount'},{key:'balance_amount',label:'Outstanding'},{key:'status',label:'Status'}]} /><WorkflowAction title="Record customer payment" endpoint={`/sales/invoices/${id}/payments`} submitLabel="Post payment and update receivable" fields={[{key:'amount',label:'Amount',type:'number',required:true},{key:'method',label:'Payment method',required:true},{key:'reference',label:'Reference'}]} /></div>; }
