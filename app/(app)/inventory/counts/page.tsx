'use client';
import { OperationalWorkspace } from '@/components/operational-workspace';
export default function Page() {
  return <div className="space-y-6"><OperationalWorkspace title="Physical Counts" description="Open a count, record each item, submit, approve and post the variance. Posting rejects stock that changed after counting." endpoint="/closure/physical-counts" columns={['id','count_number','warehouse_id','status','opened_at','posted_at']} statuses={['open','submitted','approved','cancelled']} action={{ label: 'Post count', path: '/closure/physical-counts/:id/post' }} create={{fields:[{key:'count_number',label:'Count number',required:true},{key:'warehouse_id',label:'Warehouse',required:true}]}} /><OperationalWorkspace title="Counted Items" description="System quantity is captured from the tenant stock ledger; enter the actual counted quantity, including zero." endpoint="/closure/physical-counts/lines" columns={['count_id','item_id','system_qty','counted_qty','variance']} create={{fields:[{key:'count_id',label:'Count',required:true},{key:'item_id',label:'Item',required:true},{key:'counted_qty',label:'Counted quantity',type:'number',required:true}]}} /></div>;
}
