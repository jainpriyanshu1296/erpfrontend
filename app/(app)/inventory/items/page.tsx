'use client';
import { ModuleWorkspace } from '@/components/module-workspace';
export default function Page() { return <ModuleWorkspace title="Item Master" description="Maintain raw materials, work-in-progress and finished goods." endpoint="/inventory/items" detailPath="/inventory/items" statusOptions={['active','inactive']} columns={['item_code','item_name','category','uom_id','gst_rate','reorder_level','is_active']} fields={[{key:'item_code',label:'Item code',required:true},{key:'item_name',label:'Item name',required:true},{key:'category',label:'Category',type:'select',options:['raw_material','wip','finished_good','consumable','service'],required:true},{key:'uom_id',label:'UOM',type:'number'},{key:'gst_rate',label:'GST rate',type:'number'},{key:'reorder_level',label:'Reorder level',type:'number'}]} />; }
