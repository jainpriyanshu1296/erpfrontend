'use client';
import { ModuleWorkspace } from '@/components/module-workspace';
export default function Page() { return <ModuleWorkspace title="In-Process Quality" description="Record checks against a work order, production order, or job card." endpoint="/quality/in-process" detailPath="/quality/inspections" columns={['inspection_number','reference_id','item_id','inspected_qty','accepted_qty','rejected_qty','status','result']} fields={[{key:'reference_id',label:'Work order, production order, or job card ID',required:true},{key:'item_id',label:'Item',required:true},{key:'inspected_qty',label:'Inspected quantity',type:'number',required:true},{key:'accepted_qty',label:'Accepted quantity',type:'number'}]} />; }
